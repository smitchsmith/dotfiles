<h1> <%= link_to @album.name, edit_album_url(@album) %> - <%= link_to @band.name, band_url(@band) %></h1>

<p><%= "(#{@album.kind} album)" if @album.kind %></p>

<h2><%= link_to "Tracks", tracks_url(@album) %></h2>

<%= render "tracks/tracks" %>

<br>
<%= link_to "Edit Album", edit_album_url(@album)%>
<br><br>
<%= button_to "Delete Album", album_url(@album), method: "delete"%>